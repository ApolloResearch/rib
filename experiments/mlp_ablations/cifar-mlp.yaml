exp_name: cifar-mlp-svd
ablation_type: rib
interaction_graph_path: /mnt/ssd-interp/stefan/rib/experiments/mlp_rib_build/out/cifar-mlp-svd_rib_graph.pt
schedule:
  schedule_type: exponential
  early_stopping_threshold: null
  ablate_every_vec_cutoff: 10
  exp_base: 2.0
dtype: float32
ablation_node_layers:  # Rotate input to these modules
    - layers.0
    - layers.1
    - layers.2
dataset:
    name: CIFAR10
batch_size: 512
seed: 0
