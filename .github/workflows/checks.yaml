name: Checks

on:
  push:
    branches:
      - main
    paths-ignore:
      - '.vscode/**'
      - '.gitignore'
      - 'README.md'
      - 'ACCESS.md'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '.vscode/**'
      - '.gitignore'
      - 'README.md'
      - 'ACCESS.md'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Determine VENV_PATH and Runner Type
      id: setup-env
      run: |
        if [ -d "/home/user/actions-runner" ]; then
          echo "VENV_PATH=/home/user/actions-runner/rib-env" >> $GITHUB_ENV
          echo "::set-output name=is_self_hosted::true"
        else
          echo "VENV_PATH=${{ github.workspace }}/rib-env" >> $GITHUB_ENV
          echo "::set-output name=is_self_hosted::false"
        fi

    - name: Install mpi
      if: steps.setup-env.outputs.is_self_hosted == 'false'
      run: |
        sudo apt-get update
        sudo apt-get install -y libopenmpi-dev

    - name: Set up or use existing virtual environment
      run: |
        if [ ! -d "$VENV_PATH" ]; then
          echo "Creating a new virtual environment at $VENV_PATH."
          python -m venv "$VENV_PATH"
        else
          echo "Using existing virtual environment at $VENV_PATH."
        fi

    - name: Install or update dependencies
      run: |
        source $VENV_PATH/bin/activate
        python -m pip install --upgrade pip
        pip install ".[dev]"

    - name: Check with Black
      run: |
        source $VENV_PATH/bin/activate
        black --check --diff --line-length 100 rib rib_scripts tests

    - name: Check with isort
      run: |
        source $VENV_PATH/bin/activate
        isort --check --thirdparty wandb --profile black rib rib_scripts tests

    - name: Check unused imports with pylint
      run: |
        source $VENV_PATH/bin/activate
        pylint --disable=all --enable=unused-import --score=n rib rib_scripts tests

    - name: Check with mypy
      run: |
        source $VENV_PATH/bin/activate
        mypy rib rib_scripts

    - name: Run tests
      run: |
        source $VENV_PATH/bin/activate
        pytest --runslow -m "not skip_ci" --durations=10
