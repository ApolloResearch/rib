seed: 0
model:
  hidden_sizes:
    - 100
    - 100
  activation_fn: relu
  bias: true
  fold_bias: true
train:
  learning_rate: 0.001
  batch_size: 64
  epochs: 12
  save_dir: /mnt/ssd-apollo/models/MNIST/checkpoints
  save_every_n_epochs: null
wandb:
  project: mlp-mnist
  entity: dbra
